<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberSQL">
    <select id="getMember" resultType="com.gogo.withgo.vo.MemberVo">
    	select * from member where email=#{email} and pw=#{pw}
    </select>
    <select id="findEmail" resultType="String">
    	select email from member where email=#{email}
    </select>
    <select id="findNickname" resultType="String">
    	select nickname from member where nickname=#{nickname}
    </select>
    <insert id="join">
    	insert into member(mno, email, pw, nickname, name, phone, birth, gender)
    	values(mem_seq.nextval, #{email}, #{pw}, #{nickname}, #{name}, #{phone}, #{birth}, #{gender})
    </insert>
    <delete id="withdraw">
    	delete from member 
    	where email=#{email}
    </delete>
    
</mapper>